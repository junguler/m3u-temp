name: scrape m3u
on:
  push
jobs:
  automated-scrape:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@main

      - name: run commands
        run: |
          for j in $(cat germany.txt) ; do curl -s http://www.hit-tuner.net/en/worldwide/country/$j.html | ./htmlq -a href a | grep "../radio" | cut -c10-  >> A-$j.txt ; echo -e "$j" ; done

      - name: add & commit
        uses: EndBug/add-and-commit@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
